//! WorkerConfig — all operational parameters for the pipeline worker.
//!
//! Every parameter has a default. All defaults can be overridden at runtime
//! via environment variables — no recompile needed to tune pipeline behavior.
//! This is hard guarantee G9: no hardcoded operational parameters.

// Stub — full implementation in P1-5.
