//! colossus-pipeline/src/worker/fetcher_tests.rs
//!
//! All CANCELLED_BY_USER constant tests deleted in the test audit (Batch 10):
//! the constant is consumed only by Rust call sites that compile-check the
//! import (verified via repo-wide grep — no external dashboards/queries
//! filter on the literal string). Fetcher SQL behavior belongs to the
//! integration suite.
